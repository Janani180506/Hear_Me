# SignConnect Unified Application Configuration

# Ports
PORT_FRONTEND=5173
PORT_BACKEND=8000

# MongoDB Configuration (Optional - In-Memory fallback active if unavailable)
MONGO_URI=mongodb://localhost:27017/touchspeak

# Secret Keys
JWT_SECRET=signconnect-dev-secret-key-2026

# Optional External API Keys (Defaults to free local gTTS / Web Speech API fallbacks)
GOOGLE_TTS_API_KEY=
GOOGLE_MAPS_API_KEY=
